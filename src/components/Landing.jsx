import "../Landing.css";

const Landing = () => {
  return (
    <section className="landing-section">
      <div className="page-container">
        <article id="landing-body">
          <p id="landing-p1">
            This was a first attempt at building a full-stack app during the
            Northcoders course, I developer an SQL database back-end and API to
            access it using this front end interface, I will continue to make
            updates to the UI when I can!
          </p>
        </article>
      </div>
    </section>
  );
};

export default Landing;
